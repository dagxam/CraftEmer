resource-pack:
  enabled: true
  # Leave empty for automatic local URL detection.
  # If the server is public, set host to the public IP or DNS name if server-ip is blank.
  url: ""
  host: ""
  port: 8123
  # Players automatically receive and must accept the resource pack.
  required: true

repair:
  # Durability restored by one emerald in an anvil.
  amount-per-emerald: 250
